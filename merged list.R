# Create two lists
list1 <- list(a = 1, b = 2)
list2 <- list(c = 3, d = 4)

# Merge the lists
merged_list <- c(list1, list2)

# Print the merged list
print(merged_list)

# Access the elements of the merged list
print(merged_list$a)
print(merged_list$c)